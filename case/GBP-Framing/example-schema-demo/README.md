# Example Schema Demo

This folder stores a synthetic example instance for `GBP Framing`.

Contents:

- `neutral_structural_model_example.json`
- `validation_report.md`
- `revit_review_import_example.dxf`

Important:

- This is a schema and tooling demo.
- It is not a recognition result derived from `Figure Q`.
- It exists to validate the V1 neutral model contract, deterministic validation, and review-DXF generation.
